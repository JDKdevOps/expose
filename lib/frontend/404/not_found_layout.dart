import 'package:flutter/material.dart';

class NotFoundLayout extends StatelessWidget {
  final Widget widget;

  const NotFoundLayout({Key? key, required this.widget}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return widget;
  }
}
